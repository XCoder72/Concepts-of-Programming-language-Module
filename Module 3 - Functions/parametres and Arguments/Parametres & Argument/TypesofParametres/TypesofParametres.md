* Default Parametres and Argument:-
A default parameter gives a function a value automatically when the caller does not provide that argument.

Argument provided    → use provided value
Argument not provided → use default value

A parameter with a default value cannot be followed by a parameter without a default value in the same parameter list.

* Practice Problem

Easy
Create greet(name="Student").
Create square(n=5).

Medium
Create:
calculateBill(price, quantity=1)
Create:
createProfile(name, age=18, country="India")

Challenge
Create a calculateSalary() function:
basicSalary
bonus = 0
tax = 0
Return:
basicSalary + bonus - tax